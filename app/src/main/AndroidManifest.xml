<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.handy.selector.app">\

    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".button.SmoothButtonActivity"/>
        <activity android:name=".dialog.DialogActivity"/>
        <activity android:name=".picker.PickerActivity"/>
        <activity android:name=".picker.NestActivity"/>
        <activity android:name=".spinner.SpinnerActivity"/>
        <activity android:name=".materialdesign.demo.MaterialDesignActivity"/>
    </application>

</manifest>